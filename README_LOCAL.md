# 🚀 Dark Haven - Локальный запуск

## Быстрый старт

### Windows
Просто запустите файл `start.bat` двойным кликом

### macOS / Linux
```bash
chmod +x start.sh
./start.sh
```

---

## Ручной запуск

### 1️⃣ Установка Node.js
Если не установлен, скачайте с [nodejs.org](https://nodejs.org/) (версия 16 или выше)

### 2️⃣ Установка зависимостей
```bash
npm install
```

### 3️⃣ Запуск сервера разработки
```bash
npm run dev
```

Сайт будет доступен по адресу: **http://localhost:5173**

---

## Создание статического билда

Если нужна версия для размещения на хостинге:

```bash
# Создать билд
npm run build

# Установить HTTP сервер (один раз)
npm install -g serve

# Запустить билд
serve -s dist -p 3000
```

Билд будет доступен по адресу: **http://localhost:3000**

---

## База данных

Все данные сохраняются локально в файле:
```
public/db/storage.json
```

Можно редактировать этот файл вручную или использовать панель управления БД в админ-панели.

---

## Структура проекта

```
dark-haven/
├── src/              # Исходный код React приложения
├── public/           # Статические файлы (музыка, изображения)
│   └── db/          # Локальная база данных
├── backend/          # Серверные функции (не используются локально)
├── start.bat         # Запуск на Windows
├── start.sh          # Запуск на macOS/Linux
└── package.json      # Зависимости проекта
```

---

## Часто задаваемые вопросы

**Q: Можно ли просто открыть HTML файл?**  
A: Нет, проект использует React и требует сервера разработки

**Q: Как остановить сервер?**  
A: Нажмите `Ctrl+C` в терминале

**Q: Где сохраняются данные?**  
A: В файле `public/db/storage.json` и localStorage браузера

**Q: Как сбросить все данные?**  
A: Используйте кнопку "Очистить" в панели управления БД (нужны права админа)

---

## Поддержка

Если возникли проблемы:
1. Убедитесь что установлен Node.js версии 16+
2. Удалите папку `node_modules` и запустите `npm install` заново
3. Проверьте что порт 5173 свободен

---

**Создано с помощью poehali.dev** 🚀
